services:
  verl:
    build: .
    image: toolrl-verl:latest
    container_name: verl
    runtime: nvidia
    shm_size: 32gb
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - PYTHONUNBUFFERED=1
    volumes:
      - hf_cache:/root/.cache/huggingface
      - models:/app/models
      - .:/workspace
    command: sleep infinity

  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.9.0
    container_name: mlflow
    ports:
      - "5000:5000"
    environment:
      - MLFLOW_SERVER_ALLOWED_HOSTS=*
    volumes:
      - mlflow_data:/mlflow
    command: mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri /mlflow

volumes:
  hf_cache:
    name: toolrl_hf_cache
    external: true
  models:
    name: toolrl_models
    external: true
  datasets:
    name: toolrl_datasets
    external: true
  mlflow_data:
    name: toolrl_mlflow_data
    external: true
